export enum Events {
    Nav = 'navEvent',
    Notify = 'notifyEvent',
    Popup = 'popupEvent',
    ComponentReady = 'componentReadyEvent'
}
export enum Status {
    SUCCESS,
    INFO,
    WARN,
    ERROR
}