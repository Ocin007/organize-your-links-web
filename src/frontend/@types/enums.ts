export enum EventType {
    Nav = 'navEvent',
    Notify = 'notifyEvent',
    Popup = 'popupEvent',
    ComponentReady = 'componentReadyEvent'
}
export enum Status {
    SUCCESS,
    INFO,
    WARN,
    ERROR
}