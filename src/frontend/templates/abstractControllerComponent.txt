import html from "./{{tag-name}}.html";
import scss from "./{{tag-name}}.scss";
import Component from "{{path-up}}../component";
import ControllerComponentInterface from "{{path-up}}../ControllerComponentInterface";
import {{ClassName}}Controller from "./{{ClassName}}Controller";

abstract class Abstract{{ClassName}} extends Component implements ControllerComponentInterface {

    private readonly _controller: {{ClassName}}Controller;

    get controller(): {{ClassName}}Controller {
        return this._controller;
    }

    constructor() {
        super(html, scss);
        this._controller = new {{ClassName}}Controller(this);
    }
}

export default Abstract{{ClassName}};